# LAMBDA FUNCTION is a small, nameless function written in one line, used for short, simple operations

# normal function VS lambda:

"""
normal function:

def func(a, b):
   return a + b
"""
"""
lambda function:

add = lambda a, b: a + b
"""

#structure of lambda: lambda parameteres: expression

# example:
square = lambda x: x*x
print(square(4))